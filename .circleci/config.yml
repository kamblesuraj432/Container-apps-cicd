version: 2.1
jobs:
  build:
    machine: true
    resource_class: kamblrsuraj432/testclass
    steps:
      - run: echo $USER && whoami
  test:
    machine: true
    resource_class: kamblrsuraj432/testclass
    steps:
      - run: |
          sudo cd /home/ubuntu
          sudo mkdir suraj

workflows:
    testing:
       jobs:
        - build
        - test
