version: 2.1
jobs:
  build:
    docker:
      - image: cimg/base:current
    resource_class: kamblrsuraj432/testclass
    steps:
      - run: echo "Hi I'm on Runners!"

workflows:
    testing:
       jobs:
        - build
